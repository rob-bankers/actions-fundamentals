name: Hello World compsite 
on:
  push:
    branches: [ main ]
    paths: [ hello-world-composite-action/** ]
  workflow_dispatch:

jobs:
  Hello-World_compsite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - id: foo
        uses: rob-bankers/my-first-action@02fe4862eadf784742bd64a63c8f04000feab895
        with:
          who-to-greet: 'Bankers'
