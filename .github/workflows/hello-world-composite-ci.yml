name: Hello World compsite 
on:
  push:
    branches: [ main ]
    paths: [ hello-world-composite-action/** ]
  workflow_dispatch:

jobs:
  Hello-World_compsite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - id: foo
        uses: rob-bankers/my-first-action@67335c9ec374c7897dff0b0c8111168c34afcd21
        with:
          who-to-greet: 'Bankers'
